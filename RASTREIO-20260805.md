# Rastreio da conversa - 2026-08-05

## Como usar este arquivo
Checkpoint técnico denso, escrito para uma IA (Claude ou outra) retomar o
projeto rapidamente sem precisar reler o histórico inteiro do git ou do chat.
Prioriza decisões, causas-raiz e armadilhas já mapeadas. Continuação direta de
`RASTREIO-20260802.md`.

## Contexto no início da sessão
- Último commit era `be269a3` (02/08): rastreio + relatório da sessão das ações
  perdidas durante o upload.
- Relato do usuário, três itens: (1) aulas fora de ordem no diário, (2)
  Registros só mostra as recentes, quer botão "ver todas", (3) "ainda estou
  tendo problema no salvamento, o que eu vejo no tablet é diferente do que eu
  vejo no meu pc, isso é inaceitável".
- **O item 3 era o risco já anotado no fim de `RASTREIO-20260802.md`**
  ("mergeBackupData mantém o valor do aparelho local em conflito de nota... é o
  próximo risco real da lista"). Era ele, e o alcance era maior que só nota.

## 1) Ordem das aulas: array nunca ordenado

`classLessons` ([main.jsx:3077](src/main.jsx)) era `data.lessons` filtrado, sem
`sort`. A ordem do array era o histórico de montagem, e cada aparelho monta
diferente:

- `saveLesson` faz `[lesson, ...current.lessons]` → aula local entra no **topo**.
- `applyFieldPatch` (sync-engine) devolve `[...map.values()]` → aula que chega
  por ação entra no **fim**.
- `mergeBackupData` faz `lessons.push(...)` → idem.

Com `classLessons.slice(0, 8)` em Registros, dois aparelhos com dados idênticos
cortavam aulas diferentes. Parte do "tablet ≠ PC" era isso, não perda de dado.

### Fix
`compareLessonsNewestFirst` em `sync-engine.js`, aplicado **dentro de
`migrateData`** ([main.jsx:427](src/main.jsx)) — ponto único por onde passa
tudo que entra no app (`loadData`, `applyPatch`, `mergeBackupData`,
importação). Ordem: `date` desc → `createdAt` desc → `id` asc.

O desempate por `id` não é decoração: sem ele, duas aulas do mesmo dia sem
`createdAt` ficavam na ordem que cada aparelho por acaso tinha.

Efeito colateral bom: `stableStringify` de array é sensível a ordem, então o
hash de integridade dos dois aparelhos passa a convergir com mais frequência —
menos entradas no ramo de merge de `loadLatestFromSupabase`.

⚠️ A aba "Diário" da planilha (`data.lessons` cru, [main.jsx:4940](src/main.jsx))
herdaria a ordem decrescente. Para planilha a leitura esperada é cronológica,
então ali há um `.slice().reverse()` explícito. Se a ordem canônica mudar, esse
`reverse` mente.

## 2) Causa raiz do item 3: `mergeBackupData` só sabia ADICIONAR

O ramo de aula já existente era literalmente só isto:

```js
const existingStudents = new Set(targetLesson.attendance.map((r) => r.studentId));
for (const record of remappedAttendance) {
  if (!existingStudents.has(record.studentId)) {
    targetLesson.attendance.push(record);
    attendanceRecordsMerged += 1;
  }
}
```

`content`, `periods`, `date` nunca eram atualizados, e status de aluno já
presente nunca era trocado. Nota preenchida × nota preenchida sempre ficava com
o aparelho local (`mergeGradeValueDetailed`, ramo `conflict`).

### Por que isso apagava dado no SERVIDOR, não só na tela

`compactToSnapshot` ([main.jsx:2746](src/main.jsx)):

```
merged = mergeBackupData(dataRef.current, reconstructedRemote).data   // local vence
saveRemoteState(userId, payload)                                       // grava merged
deleteActions(userId, pending.map(r => r.id))                          // APAGA as acoes
```

Linha do tempo que destrói a correção:

```
tablet   edita aula-1: content "A" -> "B", aluno x present -> absent
tablet   acao P sobe pro servidor
PC       compactToSnapshot: reconstroi remoto com P (aula-1 = "B"/absent)
PC       mergeBackupData(local, remoto) -> aula-1 ja existe -> mantem "A"/present
PC       grava "A"/present em diario_current
PC       deleteActions([P])        <- a correcao do tablet deixa de existir
```

O mesmo vale para o ramo de merge de `loadLatestFromSupabase`, que também grava
com `forceSnapshot: true` e chama `deleteActions(appliedActionIds)`.

### Fix: regra de recência, em funções puras

Novas exportações em `src/sync-engine.js` (testáveis em node, seguindo a
decisão de 02/08 de tirar o motor de sync do componente React):

- `recordStamp(item)` → `updatedAt ?? createdAt`, via `toTime` (null se inválido).
- `incomingIsNewer(current, incoming)` → estrito. Empate ou carimbo ausente do
  lado que chega ⇒ `false` (mantém a tela).
- `mergeAttendance(currentList, incomingList, incomingWins)` → união por
  `studentId`, sem mutar as listas recebidas. Devolve `{ attendance, added, updated }`.
- `mergeLessonPair(current, incoming)` → resolve `LESSON_SCALAR_FIELDS`
  (`date`, `periodId`, `periods`, `content`) + chamada. Devolve
  `{ lesson, fieldsUpdated, attendanceAdded, attendanceUpdated }`.

`mergeGradeValueDetailed` ganhou 3º parâmetro `incomingWins` (default `false` =
comportamento antigo). Ligado nos 2 call sites de avaliação e no de
recuperação, com `incomingIsNewer(target, imported)`.

Os carimbos já existiam e não eram usados: `updatedAt` é gravado em
`saveLesson` (edição), `updateGrade` ([main.jsx:4592](src/main.jsx)), edição de
avaliação e recuperação.

⚠️ Quando o lado que chega vence, o `updatedAt` dele **tem que ser copiado
junto** (aula, avaliação e recuperação fazem isso). Sem copiar, o próximo merge
compara contra o carimbo velho e desfaz a atualização que acabou de aplicar.

⚠️ `LESSON_SCALAR_FIELDS` **não** inclui `id` nem `className`: `className` é
resolvido pelo `classIdMap` de quem chama, e o casamento por fallback
(classId+date+periods+content) pode parear aulas com ids diferentes — copiar
`id` ali trocaria a identidade da aula.

⚠️ `Object.assign(targetLesson, resolved.lesson)` é intencional: `targetLesson`
é elemento do array `lessons` já copiado no topo da função, e o resto do laço
continua achando a mesma referência.

## 3) Decisão de projeto: status real ganha de "não chamada", ANTES da data

Em `mergeAttendance` a ordem de precedência é:

1. aluno que só existe de um lado → entra (nunca perde aluno);
2. status real (`present`/`absent`/`excused`) ganha de `not-taken`, **mesmo com
   o carimbo contra**;
3. só entre dois status reais é que `incomingWins` decide.

Motivo de (2) vir antes de (3): o carimbo é da **aula inteira**, não por aluno.
Sem essa precedência, editar o conteúdo de uma aula num aparelho que nunca
recebeu a chamada bastaria para apagar faltas já lançadas no outro.

Custo aceito e comunicado ao usuário: existe botão "Não chamada" por aluno
([main.jsx:6990](src/main.jsx)), e marcar de volta "Não chamada" num aparelho
**não** apaga a chamada no outro. Assimetria deliberada — perder falta é
estrago em documento oficial, sobrar chamada é visível e refazível.

Se um dia houver carimbo por registro de chamada, (2) pode cair e (3) resolve
tudo. É a evolução natural.

## 4) Testes novos

`scripts/sync-engine.test.mjs` ganhou ~15 asserções: `incomingIsNewer` (4
casos), `mergeAttendance` (6, incluindo não-mutação da entrada), `mergeLessonPair`
(2, ida e volta), `compareLessonsNewestFirst` (2, sendo uma que ordena a MESMA
lista embaralhada de dois jeitos e exige resultado igual).

**Validado que os testes prestam**: script descartável no scratchpad
reimplementando o ramo antigo (`mergeLessonPairAntigo`) — conteúdo, `periods` e
status ficam todos no valor velho, então as asserções novas reprovam. Diferente
de 02/08, a prova não dependeu de exit code: o script imprime os 3 valores
resolvidos.

## 5) `scripts/sync-supabase.test.mjs` — integração contra Supabase real

Fecha a pendência "caminhos de rede não cobertos por teste" que aparecia em
todos os rastreios anteriores. Stack local do CLI (Postgres + PostgREST +
GoTrue reais), `supabase/schema.sql` do projeto aplicado tal e qual.

Setup (não commitado — fica no scratchpad da sessão):

```
mkdir supa-local && cd supa-local && npx supabase init
npx supabase start -x realtime,storage-api,imgproxy,studio,edge-runtime,logflare,vector,supavisor,mailpit
docker exec -i -e PGCLIENTENCODING=UTF8 supabase_db_supa-local \
  psql -v ON_ERROR_STOP=1 -U postgres -d postgres < supabase/schema.sql
eval "$(npx supabase status -o env | sed 's/^/export /')"
export SUPABASE_URL=$API_URL SUPABASE_ANON_KEY=$ANON_KEY SUPABASE_SERVICE_ROLE_KEY=$SERVICE_ROLE_KEY
npm run test:supabase
```

Sem as 3 variáveis o teste imprime `PULADO` e sai 0 — `npm test` não quebra pra
quem não tem Docker. Já está agregado no `npm test`.

`src/supabase.js` **não é importável em node** (`import.meta.env` +
`window.sessionStorage`), então o teste espelha as queries à mão. Elas têm que
ser mantidas em sincronia com o arquivo real — mesmo `select`, mesmo `eq/gt`,
mesmo `onConflict`, e o `fetch` cru com `keepalive` para `appendAction`.

### Achados que só o banco real dá

**(a) O gatilho `before update` medido, não assumido.** `updated_at` gravado em
`10:25:07.883954` para um pacote montado em `10:25:06.760`. É a causa raiz de
01/08 comprovada em Postgres, não mais inferida do schema.

**(b) O PostgREST devolve `created_at` com MICROSSEGUNDO**:
`2026-08-05T10:25:08.297014+00:00`. `Date.parse` só tem milissegundo.

Isso torna crítico algo que hoje funciona por acidente: `nextActionsCutoff`
devolve a **string crua** do banco e ela volta como filtro `.gt()`, comparada
lá dentro em microssegundo. O teste prova que remontar a data
(`new Date(x).toISOString()`) faz a marca andar pra trás e **reentrega 1 ação**
— ou seja, reprocessamento eterno da mesma alteração. Havia zero proteção
contra alguém "normalizar" essa string. Agora há.

⚠️ Corolário conhecido e aceito: `toTime()` em `nextActionsCutoff` compara em
milissegundo, então duas ações no mesmo ms com microssegundos diferentes
empatam e a marca fica na primeira (a lista vem `ascending`). Resultado é
reentrega da(s) irmã(s) do mesmo milissegundo — inofensivo (patch é por id,
idempotente) e praticamente inalcançável, já que `AUTO_SYNC_DELAY_MS` é 2s.

**(c) `anon` barrado com Postgres 42501** (`insufficient_privilege`), ou seja no
`revoke`, antes mesmo do RLS. E professor autenticado lendo a linha de outro
recebe 0 linhas.

### Prova de que o teste presta

Caso 4 reexecutado com o ramo antigo de `mergeBackupData` (`mergeAntigo` no
scratchpad), pelo transporte real: `conteudo: Conteudo original | periods: 2 |
chamada: present` → **AssertionError**. Seguindo a lição de 02/08, o script
verifica o TIPO do erro. A primeira tentativa morreu em `ERR_MODULE_NOT_FOUND`
(script no scratchpad não resolve `@supabase/supabase-js`) — rodar de dentro do
projeto e apagar o arquivo depois.

⚠️ O teste de integração cobre `mergeLessonPair`, **não** `mergeBackupData`
inteira — esta continua presa em main.jsx. Extrair a mesclagem para o
sync-engine é o próximo passo natural, mas ela arrasta `migrateData`,
`lessonPeriods`, `normalizeKey`, `isMissingGrade` e `assessmentAllowsMakeup`
junto.

## Ambiente: o que existe e o que não existe (verificado em 05/08)
- **Claude in Chrome: não conectado.** `list_connected_browsers` → `[]`.
- **Supabase CLI: instalado via npx (2.111.0), sem token.** `supabase login` é
  interativo. Continua sem acesso ao projeto de produção.
- **Chave anon do `.env` → 401 nas 3 tabelas** (confirmado por curl). Endpoint
  de auth responde 200, ou seja o projeto está no ar.
- **Docker: instalado, daemon parado por padrão.** Sobe com
  `Start-Process "$env:ProgramFiles\Docker\Docker\Docker Desktop.exe"`; leva
  ~1min. As imagens do Supabase já estão baixadas nesta máquina.
- ⚠️ A máquina roda outros containers (`whaticket-*`). `supabase stop` só mexe
  nos `supabase_*_supa-local` — mas não usar `docker stop`/`prune` global.

## Armadilhas técnicas para sessões futuras
- Herdadas e ainda válidas: SW só toca em arquivos do próprio site (rodar
  `npm run test:sw` em qualquer mexida); `IS_LOCAL_DEV` desliga o SW em
  localhost; nunca usar pipe de texto cru do PowerShell em arquivo com
  acentuação; `applyPatch/applyPatches` não migram sem o 3º argumento.
- **Sem credencial de banco disponível para o Claude** (só a chave publicável,
  RLS rejeita). Continua valendo.
- **O app exige sessão do Supabase para desbloquear** (`appUnlocked = !!session`),
  mas o `<main>` renderiza por baixo do overlay de login. Dá para verificar UI
  sem logar: semear `localStorage["checkout-turmas:v3"]` com `{ data: {...} }`
  e ler o DOM. Foi assim que a ordenação e o botão foram conferidos nesta
  sessão. `loadData` desembrulha `parsed?.data ?? parsed`.
- **`createRoot` é chamado no topo do módulo** ([main.jsx](src/main.jsx), fim do
  arquivo). Cada HMR do Vite re-executa e loga
  "createRoot() on a container that has already been passed to createRoot()".
  É ruído de dev, não existe em build. Não confundir com erro real ao ler o
  console em `npm run start`.
- **Ordem canônica das aulas agora é contrato**, não detalhe de tela. Qualquer
  lugar que precise de ordem crescente tem que reverter explicitamente (só a
  aba "Diário" da planilha faz isso hoje).

## Estado atual (fim da sessão, 2026-08-05)
- `npm test` OK (**5 suítes**, incluindo a nova de integração contra Supabase
  real), `npm run build` OK.
- Ordenação e botão "Ver todas as aulas" verificados no navegador com 12 aulas
  em ordem embaralhada; dados de teste removidos depois.
- **Produção não foi tocada em momento nenhum.** Toda a integração rodou na
  stack local, com usuários descartáveis apagados no fim.
- Commitado e publicado no `main` (deploy automático da Vercel).

## Pendências e riscos conhecidos (não corrigidos nesta sessão)
- **Sem carimbo por item de chamada e por nota individual.** A recência é por
  aula / por avaliação. Duas edições simultâneas em alunos diferentes da mesma
  avaliação ainda resolvem em bloco pelo `updatedAt` da avaliação — quem salvou
  por último leva os dois. Melhor que "o local sempre vence", mas não é CRDT.
- **`summarizeSyncImpact` não conta `lessonsUpdated`.** A tela de revisão manual
  de sincronização (usada quando a integridade não confere) segue mostrando só
  os totais antigos. `mergeBackupData` já devolve o campo.
- **Deleção não sobrevive à mesclagem.** Aula apagada num aparelho volta pelo
  merge do outro (`mergeBackupData` nunca remove). É proteção deliberada contra
  perda, mas é divergência entre aparelhos — não relatada ainda pelo usuário.
- **`mergeBackupData` ainda não é testável** (presa em main.jsx). A integração
  cobre `mergeLessonPair`, que é o miolo, não a função inteira.
- **Produção nunca foi verificada por ninguém além do próprio app.** Sem token
  de CLI e sem extensão do Chrome, o estado real do banco do usuário continua
  inacessível para o Claude. Se algum dia for preciso, o caminho limpo é o
  usuário rodar `supabase login` no terminal dele.
- **`compactToSnapshot()` ainda falha em silêncio** no caminho automático
  (`.catch(console.warn)`, sem `await`).
- APK Android continua defasado (junho), só a web está atualizada.
